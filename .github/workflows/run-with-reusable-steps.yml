name: Ejecutar Script Externo

on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  call-workflow-passing-data:
    runs-on: ubuntu-latest
    steps:
      - name: Call reusable workflow passing data
        uses: jmorenocastillo/clonerepo/.github/workflows/blank.yml@main
        with:
          VAR1: "test"
          VAR2: "valor2"
          GITHUB_USER: ${{ github.actor }}
          SCRIPT_PATH: "repo-scripts/scripts/deploy.sh"

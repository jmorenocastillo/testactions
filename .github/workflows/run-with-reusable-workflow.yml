name: Run with reusable workflow

on:
  workflow_dispatch: # Manually trigger the workflow

    inputs:
      dockerfile:
        type: string  

jobs:
  jobid:
    runs-on: ubuntu-latest
    env:
      BUILD_SECRETS: ${{ secrets[format('{0}_BUILD_SECRETS', inputs.dockerfile)] }}
    steps:
    - run: echo "$BUILD_SECRETS"